# ========== FRONTEND (this repo root / Vite) ==========
# Optional: override API base (dev uses /api proxy to backend by default)
# VITE_API_URL=http://localhost:5000/api

# ========== BACKEND (separate - see backend/.env) ==========
# Run backend from backend/ with: npm run dev
# Backend must be running for login and API to work. See backend/.env.example.
